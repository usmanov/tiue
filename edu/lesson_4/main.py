class Computer:
    #stuff can be only Atributes (variables) and Behaviour (Methods)
    #what every has?
    def config (self):
        print("i5, 16gb, 1TB")

comp = Computer()

Computer.config(comp)
comp.config()